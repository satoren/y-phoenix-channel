{
  "name": "assets",
  "version": "1.0.0",
  "description": "",
  "main": "js/app.js",
  "scripts": {
    "build": "node build.js --deploy",
    "test": "vitest",
    "format": "npx @biomejs/biome format --write ./",
    "install:all": "npm install && cd js/quill && npm install && cd ../excalidraw && npm install && cd ../js-draw && npm install && cd ../lexical && npm install && cd ../prosemirror && npm install && cd ../tiptap && npm install"
  },
  "type": "module",
  "author": "",
  "license": "MIT",
  "dependencies": {
    "lib0": "0.2.114",
    "topbar": "3.0.0",
    "y-indexeddb": "9.0.12",
    "y-protocols": "1.0.6",
    "y-utility": "0.1.4",
    "yjs": "13.6.27"
  },
  "devDependencies": {
    "@types/phoenix": "1.6.6",
    "@types/react": "19.1.13",
    "@types/react-dom": "19.1.9",
    "esbuild": "0.25.12",
    "esbuild-sass-plugin": "3.3.1",
    "vitest": "4.0.7"
  }
}
