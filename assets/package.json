{
  "name": "assets",
  "version": "1.0.0",
  "description": "",
  "main": "js/app.js",
  "scripts": {
    "build": "node build.js --deploy",
    "test": "vitest",
    "format": "npx @biomejs/biome format --write ./",
    "install:all": "npm install && cd js/quill && npm install && cd ../blocknote && npm install && cd ../excalidraw && npm install && cd ../js-draw && npm install && cd ../lexical && npm install && cd ../prosemirror && npm install && cd ../tiptap && npm install"
  },
  "type": "module",
  "author": "",
  "license": "MIT",
  "dependencies": {
    "topbar": "3.0.0"
  },
  "devDependencies": {
    "@types/phoenix": "1.6.6",
    "@types/react": "19.2.7",
    "@types/react-dom": "19.2.3",
    "esbuild": "0.27.1",
    "esbuild-sass-plugin": "3.3.1",
    "vitest": "4.0.15"
  }
}
